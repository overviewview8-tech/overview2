# Example environment variables for local development
# DO NOT put real secrets in this file if committing to source control.
# Create a local copy named `.env.local` (gitignored) and add your real values there.

SMTP_HOST=smtp.gmail.com
# SMTP_PORT: 587 for STARTTLS, 465 for TLS
SMTP_PORT=587
SMTP_USER=overviewview8@gmail.com
SMTP_PASS=dxav spkh mija tckp
SMTP_SECURE=false
FROM_EMAIL=overviewview8@gmail.com

# Optional: increase logging for debugging
#DEBUG_EMAIL=true
